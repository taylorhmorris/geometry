/**
 * A class for a `Point`
 * 
 * @alpha
 */
export class Point {
  /** The x coordinate of the `Point` */
  x: number;
  /** The y coordinate of the `Point` */
  y: number;

  constructor(x: number, y: number) {
    this.x = x;
    this.y = y;
  }
}
